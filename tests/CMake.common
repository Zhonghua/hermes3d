# !!! Keep the order of packages and libraries unless linking would be broken !!!
# !!! Do NOT edit the lines below UNLESS you are REALLY SURE what you are doing !!!
#

# Judy
set(TESTS_INCLUDE_DIRS ${TESTS_INCLUDE_DIRS} ${JUDY_INCLUDE_DIR})
set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${JUDY_LIBRARY}) 

# UMFPACK
if(WITH_UMFPACK)
	set(TESTS_INCLUDE_DIRS ${TESTS_INCLUDE_DIRS} 
		${UFCONFIG_INCLUDE_DIR}
		${UMFPACK_INCLUDE_DIR}
		${AMD_INCLUDE_DIR}
	)
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES}
		${UMFPACK_LIBRARY}
		${AMD_LIBRARY}
	)

	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${BLAS_LIBRARIES})
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${LAPACK_LIBRARIES})
endif(WITH_UMFPACK)

# PARDISO
if(WITH_PARDISO)
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${PARDISO_LIBRARY})
endif(WITH_PARDISO)

# PETSc (SLEPC needs PETSc)
if(WITH_PETSC OR WITH_SLEPC)
	set(TESTS_INCLUDE_DIRS ${TESTS_INCLUDE_DIRS} ${PETSC_INCLUDE_DIRS})
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${PETSC_LIBRARIES})
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${PETSC_LIBS})

	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${BLAS_LIBRARIES})
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${LAPACK_LIBRARIES})
endif(WITH_PETSC OR WITH_SLEPC)

# SLEPC
if(WITH_SLEPC)
	set(TESTS_INCLUDE_DIRS ${TESTS_INCLUDE_DIRS} ${SLEPC_INCLUDE_DIRS})
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${SLEPC_LIBRARY})
endif(WITH_SLEPC)

# HDF5
if(WITH_HDF5)
	set(TESTS_INCLUDE_DIRS ${TESTS_INCLUDE_DIRS} ${HDF5_INCLUDE_DIR})
	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${HDF5_LIBRARY})

#	# ZLIB
#	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${ZLIB_LIBRARY})
endif(WITH_HDF5)


#if(USE_GLUT)
#	set(TESTS_INCLUDE_DIRS ${TESTS_INCLUDE_DIRS} ${GLUT_INCLUDE_DIR})
#	set(TESTS_LIBRARIES ${TESTS_LIBRARIES} ${GLUT_LIBRARY})
#endif(USE_GLUT)

